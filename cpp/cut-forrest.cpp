#include <bits/stdc++.h>
using namespace std;

int cutOffTree(vector<vector<int>> &forest)
{

    int count = 0, posX = 0, posY = 0;

    for (int i = 0; i < forest.size();)
    {
        for (int j = 0; j < forest[i].size;)

        {
            int n, m;
        }
    }
}

int main()
{

    return 0;
}